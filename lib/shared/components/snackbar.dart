// import 'package:flutter/material.dart';
// import 'package:provider/provider.dart';
// import 'package:sims_ppob_ainul_muhlasin/core/remote/base/base_controller.dart';

// class MySnackBar extends StatelessWidget {
//   const MySnackBar({super.key});

//   @override
//   Widget build(BuildContext context) {
//     final snackBarProvider = Provider.of<BaseController>(context);

//     if (!snackBarProvider.isVisible) return const SizedBox();

//     return SnackBar(
//       content: Text(snackBarProvider.message),
//       backgroundColor: Colors.red,
//       duration: const Duration(seconds: 3), // Adjustable if needed
//     );
//   }
// }
